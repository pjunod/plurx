[package]
name = "plurx-pgs"
description = "Bounded raw-SUP parser and normalizer for Plurx PGS overlays"
version.workspace = true
edition.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
# Milestone 0 audited this exact release. Keep the production parser pin exact;
# upgrades repeat the source, fuzz, RustSec, and representative-media review.
libpgs = "=0.6.0"
hex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true

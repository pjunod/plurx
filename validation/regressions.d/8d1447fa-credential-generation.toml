version = 1

[[coverage]]
commits = ["8d1447fa"]
points = ["identity.access", "core.media"]
checks = ["rust-gate"]
reason = "The corrective commit replaces the derived Debug on CredentialGeneration with a manual impl that prints only CredentialGeneration(<redacted>) and removes unused Serialize/Deserialize derivations so the digest cannot escape through any supported serialization route. The retained regression proves format!(\"{:?}\", generation) returns the redacted form and never contains the raw hex digest; the removed derives are a compile-time evidence gap that passing serialized CredentialGeneration onto any supported code path now requires an explicit derive."

version = 1

[[coverage]]
commits = ["d4bd4eb7", "c764d6f5", "a010e064", "615be97f"]
points = ["cluster.membership", "web.experience", "operations.packaging"]
checks = ["rust-gate", "cluster-auth", "web-membership", "operations-contract", "resource-bounds"]
reason = "The retained cluster harness starts from the origin/main membership table with duplicate shared join URLs, proves rolling voters converge to distinct node-specific artwork addresses, refuses an attempted duplicate claim, and preserves both follower removal and leader self-leave through degraded 4→3 handoff. It also proves one-write rotating repair contention, removal-fence revocation, and tombstone-preserving heartbeats. Rust and web regressions cover singleflight/global fetch bounds, filename-bound peer proofs, short-hostname-safe local-node leave binding, and refusal rendering, while the operations contract pins the node-specific artwork URL and same-backend JSON leave procedure. The mapped integration and evidence-name corrections retain those checks across the rebased change set. Reverting the correction restores replicated-write amplification, rolling-upgrade or load-balancer ambiguity, or a removed voter that can keep using cluster-side effects."

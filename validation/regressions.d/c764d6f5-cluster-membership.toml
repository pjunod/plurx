version = 1

[[coverage]]
commits = ["c764d6f5", "615be97f", "26f35afc", "467b3dec"]
points = ["cluster.membership", "web.experience", "operations.packaging"]
checks = ["rust-gate", "cluster-auth", "web-membership", "operations-contract", "resource-bounds"]
reason = "The retained cluster harness starts from the origin/main membership table with duplicate shared join URLs, proves rolling voters converge to distinct node-specific artwork addresses, refuses an attempted active-owner duplicate, permits a fresh node identity to reuse a tombstoned owner's node-local URL, and preserves follower removal plus leader self-leave through degraded 4→3 handoff. That lifecycle exercises the complete hostname-aware node projection used by removal, and also proves one-write rotating repair contention, removal-fence revocation, and tombstone-preserving heartbeats. Rust and web regressions cover singleflight/global fetch bounds, filename-bound peer proofs, short-hostname-safe local-node leave binding, and refusal rendering, while the operations contract pins the node-specific artwork URL and same-backend JSON leave procedure. Reverting the correction restores replicated-write amplification, rolling-upgrade or load-balancer ambiguity, a rejoin lockout, an incomplete membership row panic, or a removed voter that can keep using cluster-side effects."

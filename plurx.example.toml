# plurx configuration — copy to plurx.toml (or /etc/plurx/plurx.toml).
# Every key is optional; these are the defaults. Env vars override the file:
#   PLURX_SERVER_NAME, PLURX_BIND, PLURX_DATA_DIR, PLURX_CONFIG, PLURX_LOG

[server]
# Human-visible server name.
name = "plurx"
# Address:port the HTTP API binds to.
bind = "0.0.0.0:32400"

[storage]
# Where the database and caches live. Created if missing.
data_dir = "./data"

# [cluster]
# Reserved in M0 so configs written by later clustering releases remain
# forward-compatible. Keep this dormant section commented during M0 so the
# same file can still be read by the immediately preceding release.
# raft_bind = "0.0.0.0:32401"
# api_bind = "0.0.0.0:32402"
# advertise_host = ""
# join_token_file = ""
# trusted_network = ""
